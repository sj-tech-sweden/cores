# Agent Memory

- Always check Docker Hub for the latest `nobentie/rentalcore` tag before building so the next version increments sequentially.
- After every change, build the RentalCore image with the new tag plus `latest`, push both tags to Docker Hub, and push the matching code to GitLab.
